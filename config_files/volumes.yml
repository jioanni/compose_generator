apifortress-mongo:
    volumes:
      description: The mount for the mongo innstance
      type: string
apifortress-postgres:
    volumes:
      description: The mount for the postgres instance
      type: string